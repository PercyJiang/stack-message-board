type Message {
    id: ID
    source: String
    content: String
    created: String
}

type Query {
    graphGetAll: [Message]
}

type Mutation {
    graphPost(source: String, content: String): Int
}
